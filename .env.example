# Configuración de la aplicación
# Copia este archivo como .env y configura tus valores

# JWT Secret - IMPORTANTE: Cambia este valor por uno único y seguro
# Puedes generar uno con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Expiración del token JWT (en segundos)
JWT_EXPIRES_IN=86400

# Entorno de ejecución (development, production)
NODE_ENV=development

# Base de datos
DB_PATH=./data/students-points.db

# Rate limiting (intentos de login)
MAX_LOGIN_ATTEMPTS=5
LOGIN_COOLDOWN_MINUTES=15
